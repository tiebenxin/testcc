package com.lensim.fingerchat.hexmeet.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "REST_CALL_RECORD_".
 */
public class RestCallRecord_Dao extends AbstractDao<RestCallRecord_, Long> {

  public static final String TABLENAME = "REST_CALL_RECORD_";

  /**
   * Properties of entity RestCallRecord_.<br/>
   * Can be used for QueryBuilder and for referencing column names.
   */
  public static class Properties {

    public final static Property Id = new Property(0, Long.class, "id", true, "_id");
    public final static Property FromSipNum = new Property(1, Integer.class, "fromSipNum", false, "FROM_SIP_NUM");
    public final static Property ToSipNum = new Property(2, Integer.class, "toSipNum", false, "TO_SIP_NUM");
    public final static Property IsVideoCall = new Property(3, Boolean.class, "isVideoCall", false, "IS_VIDEO_CALL");
    public final static Property StartTime = new Property(4, Long.class, "startTime", false, "START_TIME");
    public final static Property Duration = new Property(5, Long.class, "duration", false, "DURATION");
  }

  ;


  public RestCallRecord_Dao(DaoConfig config) {
    super(config);
  }

  public RestCallRecord_Dao(DaoConfig config, DaoSession daoSession) {
    super(config, daoSession);
  }

  /**
   * Creates the underlying database table.
   */
  public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
    String constraint = ifNotExists ? "IF NOT EXISTS " : "";
    db.execSQL("CREATE TABLE " + constraint + "\"REST_CALL_RECORD_\" (" + //
        "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
        "\"FROM_SIP_NUM\" INTEGER," + // 1: fromSipNum
        "\"TO_SIP_NUM\" INTEGER," + // 2: toSipNum
        "\"IS_VIDEO_CALL\" INTEGER," + // 3: isVideoCall
        "\"START_TIME\" INTEGER," + // 4: startTime
        "\"DURATION\" INTEGER);"); // 5: duration
  }

  /**
   * Drops the underlying database table.
   */
  public static void dropTable(SQLiteDatabase db, boolean ifExists) {
    String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"REST_CALL_RECORD_\"";
    db.execSQL(sql);
  }

  /**
   * @inheritdoc
   */
  @Override
  protected void bindValues(SQLiteStatement stmt, RestCallRecord_ entity) {
    stmt.clearBindings();

    Long id = entity.getId();
    if (id != null) {
      stmt.bindLong(1, id);
    }

    Integer fromSipNum = entity.getFromSipNum();
    if (fromSipNum != null) {
      stmt.bindLong(2, fromSipNum);
    }

    Integer toSipNum = entity.getToSipNum();
    if (toSipNum != null) {
      stmt.bindLong(3, toSipNum);
    }

    Boolean isVideoCall = entity.getIsVideoCall();
    if (isVideoCall != null) {
      stmt.bindLong(4, isVideoCall ? 1L : 0L);
    }

    Long startTime = entity.getStartTime();
    if (startTime != null) {
      stmt.bindLong(5, startTime);
    }

    Long duration = entity.getDuration();
    if (duration != null) {
      stmt.bindLong(6, duration);
    }
  }

  /**
   * @inheritdoc
   */
  @Override
  public Long readKey(Cursor cursor, int offset) {
    return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
  }

  /**
   * @inheritdoc
   */
  @Override
  public RestCallRecord_ readEntity(Cursor cursor, int offset) {
    RestCallRecord_ entity = new RestCallRecord_( //
        cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
        cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // fromSipNum
        cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // toSipNum
        cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0, // isVideoCall
        cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // startTime
        cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5) // duration
    );
    return entity;
  }

  /**
   * @inheritdoc
   */
  @Override
  public void readEntity(Cursor cursor, RestCallRecord_ entity, int offset) {
    entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
    entity.setFromSipNum(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
    entity.setToSipNum(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
    entity.setIsVideoCall(cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0);
    entity.setStartTime(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
    entity.setDuration(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
  }

  /**
   * @inheritdoc
   */
  @Override
  protected Long updateKeyAfterInsert(RestCallRecord_ entity, long rowId) {
    entity.setId(rowId);
    return rowId;
  }

  /**
   * @inheritdoc
   */
  @Override
  public Long getKey(RestCallRecord_ entity) {
    if (entity != null) {
      return entity.getId();
    } else {
      return null;
    }
  }

  /**
   * @inheritdoc
   */
  @Override
  protected boolean isEntityUpdateable() {
    return true;
  }

}
